using UnityEngine;

public class LockOnAnchor : MonoBehaviour
{
    // I'd rather have this script for getting the anchor point for lock on UI
    // rather than making it a property of Enemy class
}
